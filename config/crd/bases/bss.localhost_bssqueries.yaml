---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: bssqueries.bss.localhost
spec:
  group: bss.localhost
  names:
    kind: BSSQuery
    listKind: BSSQueryList
    plural: bssqueries
    shortNames:
    - bssq
    singular: bssquery
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.query
      name: Query Type
      type: string
    - jsonPath: .spec.apiEndpoint
      name: Endpoint
      type: string
    - jsonPath: .status.lastQueryTime
      name: Last Query
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BSSQuery is the Schema for the bssqueries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BSSQuerySpec defines the desired state of BSSQuery
            properties:
              apiEndpoint:
                description: APIEndpoint is the URL of the BSS API GraphQL endpoint
                type: string
              clusterID:
                description: ClusterID is the cluster ID to query (for single cluster
                  queries)
                type: string
              query:
                description: Query specifies what to query from the BSS API
                enum:
                - cluster
                - clusters
                type: string
              refreshInterval:
                default: 30
                description: RefreshInterval defines how often to refresh the query
                  results (in seconds)
                format: int32
                type: integer
            required:
            - apiEndpoint
            - query
            type: object
          status:
            description: BSSQueryStatus defines the observed state of BSSQuery
            properties:
              clusterCount:
                description: ClusterCount is the number of clusters returned (for
                  list queries)
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the BSSQuery's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastQueryTime:
                description: LastQueryTime is the timestamp of the last successful
                  query
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed BSSQuery
                format: int64
                type: integer
              result:
                description: Result contains the JSON result from the GraphQL query
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
